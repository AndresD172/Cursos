/* Para facilitar la buena compresión de nuestro código de cara a otros desarrolladores es recomendable nombrar
a nuestras variables usando la notación de altas y bajas, más conocida como la notación de camello. */

/* La notación de camello consiste en que si una variable lleva 2 palabras la primera letra de la segunda 
palabra debe de ir con mayúscula. */

let notaciónnorecomendada; // ]-> Esta notación no es recomendada para escribir un código legible.

let notaciónRecomendada; // ]-> Esta notación es recomendada para poder escribir un código legible.

/* Para evitar la creación de código spaghetti dentro de nuestros programas, JavaScript nos permite la creación
de múltiples variables en una sola linea y posteriormente asignar los valores de las variables separadamente. */

let x, y, z;

x = 2, y = 3;

z = x + y;
